[project]
name = "spark-learning-lab"
version = "0.1.0"
description = "Experiments for an MLE learning how Spark and data systems work"
requires-python = ">=3.11,<3.12"
dependencies = [
  "pyspark==3.4.4",
]

[tool.pytest.ini_options]
testpaths = ["episodes"]
